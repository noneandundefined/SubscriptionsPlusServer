sudo systemctl daemon-reload        # Перезагрузка systemd для применения изменений
sudo systemctl start SubscriptionPlusServer.service     # Запусков сервисов
sudo systemctl enable SubscriptionPlusServer.service    # Включение автозапуска при старте системы
sudo systemctl status SubscriptionPlusServer.service    # Проверяет статус сервиса

journalctl -u SubscriptionPlusServer.service -f   # Просмотр логов в реальном времени

# redis
sudo systemctl status redis
